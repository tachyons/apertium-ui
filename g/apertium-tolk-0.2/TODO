
* There is a terrible thing where we bundle our own py-compile 
  that just does nothing. This is because by default automake
  compiles python modules and debian does not like this. So
  instead we do nothing. This is '''broken'''.

* Get sh.po to generate sr, hr, bs etc.
